apiVersion: v1
kind: Service
metadata:
  name: jhipster_demo
  namespace: employee_mgmt
  labels:
    app: jhipster_demo
spec:
  selector:
    app: jhipster_demo
  type: LoadBalancer
  ports:
    - name: http
      port: 80
      targetPort: 8080
